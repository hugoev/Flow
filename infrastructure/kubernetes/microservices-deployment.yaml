# Vehicle Telemetry System - Microservices Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-ingestion-service
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: data-ingestion
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: data-ingestion-service
  template:
    metadata:
      labels:
        app: data-ingestion-service
        component: data-ingestion
        tier: backend
    spec:
      containers:
        - name: data-ingestion
          image: vehicle-telemetry/data-ingestion-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: kafka.bootstrap.servers
            - name: LOGGING_LEVEL_COM_VEHICLETELEMETRY
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: logging.level.com.vehicletelemetry
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/telemetry/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/telemetry/health
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: data-ingestion-service
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: data-ingestion
spec:
  selector:
    app: data-ingestion-service
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-processing-service
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: data-processing
    tier: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: data-processing-service
  template:
    metadata:
      labels:
        app: data-processing-service
        component: data-processing
        tier: backend
    spec:
      containers:
        - name: data-processing
          image: vehicle-telemetry/data-processing-service:latest
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: kafka.bootstrap.servers
            - name: SPRING_CASSANDRA_CONTACT_POINTS
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: cassandra.contact.points
            - name: SPRING_CASSANDRA_PORT
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: cassandra.port
            - name: SPRING_CASSANDRA_KEYSPACE_NAME
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: cassandra.keyspace
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /api/processing/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /api/processing/health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: data-processing-service
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: data-processing
spec:
  selector:
    app: data-processing-service
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: telemetry-streaming-service
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: telemetry-streaming
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: telemetry-streaming-service
  template:
    metadata:
      labels:
        app: telemetry-streaming-service
        component: telemetry-streaming
        tier: frontend
    spec:
      containers:
        - name: telemetry-streaming
          image: vehicle-telemetry/telemetry-streaming-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SERVICES_PROCESSING_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: vts-config
                  key: processing.service.url
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /api/telemetry/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/telemetry/health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: telemetry-streaming-service
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: telemetry-streaming
spec:
  selector:
    app: telemetry-streaming-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-dashboard
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: frontend
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-dashboard
  template:
    metadata:
      labels:
        app: frontend-dashboard
        component: frontend
        tier: frontend
    spec:
      containers:
        - name: frontend
          image: vehicle-telemetry/frontend-dashboard:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: frontend-dashboard
  namespace: vehicle-telemetry
  labels:
    app: vehicle-telemetry-system
    component: frontend
spec:
  selector:
    app: frontend-dashboard
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: LoadBalancer
